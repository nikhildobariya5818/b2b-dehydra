export const metadata = {
  title: "Register - De'Hydra Admin",
  description: "Create your admin account",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
